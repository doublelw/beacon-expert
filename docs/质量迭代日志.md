# 质量迭代日志（宪法 §4.5）

格式：日期 / 触发 / 23条中已实现规则的 PASS 数 / 本轮修复 / 剩余 FAIL

---

## 2026-09-14 · Round 1 · 规则引擎首跑 + 修复循环

- **触发**: 用户令「测试新规则下的生成效果，对比样例修正程序」
- **实现断言**: 9/23 条（G4 G5 D1 D2 D4 D5 A1 A2 F2），其余 14 条待实现（见宪法 §6）
- **首跑**: 5/9 PASS
  - FAIL D1（5个x坐标+1个y坐标不可达）
  - FAIL A1（15 对文字重叠）
  - FAIL A2（尺寸线穿图 112 处）
  - FAIL F2（SPCC≠Q235）
- **本轮修复**（按根因）:
  1. **D1**: 撤销 <1.2mm 微段过滤——样例最小线性 1.2mm 证明微段必标，过滤致孔坐标不可达=违宪 → D1 59/59, 39/39 全覆盖
  2. **F2**: 材料同源化 DEFAULT_MATERIAL='Q235'（标题栏/技术要求/明细栏共用）
  3. **检查器 bug**: 巨半径伪拟合弧（R=2.9e23）按整圆判→罩住全图误报 112 处；改按弦段判 → A2 降到真问题
  4. **生成器 bug（隐藏大雷）**: `_resolve_dims` 仍按老约定减 ANN_ORIGINS(250,380)，但移植版 annotator 已输出零件局部坐标——**全部标注整体错位 250mm/380mm**，这是链式尺寸漂到视图外、穿越其它视图的根因
  5. **生成器 bug**: 近直线样条被 Kasa 拟合成 R>1000 天文半径弧 → `_near_line` 判直线 + `_fit_circle` 半径上限 1000
  6. **A1 根因**: placer 的 MIN_GAP=4mm 是零件坐标，比例 0.5 缩放后=2mm<文字宽 → 正解在图纸坐标系消重叠：render_annotation 重写为两阶段（放置计算→A1+A2 联合收敛循环→emit），每轮先修穿图（换边→加层）再修重叠（推后者+13mm），单调收敛
- **终态**: **9/9 PASS**（D1 59/59+39/39 孔坐标全覆盖 · A1 零重叠 · A2 零穿图 · F2 材料一致）· 全套 19 测试绿
- **样例对比**: DIMENSION 93（样例108 的 86%）· 孔径 10/10 种（n-Ød 计数引线同款）· 孔位 100% 重建可达
- **剩余 FAIL**: 无（已实现范围内）
- **遗留（非 FAIL，后续轮次）**:
  - 14 条规则未实现断言（G1 G2 G3 G6 D3 D6 A3 A4 A5 A6 F1 F3 F4 F5）
  - 版面分布：链式 lane 偏向 Top 视图上侧堆积（样例是四面放射）——A 规则细化项
  - 后壳/前壳 DWG 样例转 DXF 后纳入判定
  - TEXT 51 vs 样例 113（孔表缺失，D 规则细化项）

## 下轮计划
1. 实现 G2 三视图对正（误差=0 断言）+ D4 扩展到 DXF 实体级（当前基于 annotation.json）
2. 后壳跑规则循环（验证通用性）
3. lane 分布均衡化（样例四面放射风格）
